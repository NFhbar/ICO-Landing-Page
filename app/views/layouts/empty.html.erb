<!DOCTYPE html>
<html>
<head>
    <%= render 'layouts/meta' %>
    <title><%= full_title(yield(:title)) %></title>
    <%= render 'layouts/rails_default' %>
    <%= render 'layouts/shim' %>

</head>
<body class="gray-bg">

    <!-- Navbar -->
    <%= render :partial => 'landing/navbar' %>

    <!-- Devise Flash Messages -->
    <!-- For landing page, messages in land.html.erb -->

    <!-- Main view  -->
    <%= yield %>

    <!-- debuggin box -->
    <%= debug(params) if Rails.env.development? %>

    <%= yield :javascript %>

</body>
</html>
